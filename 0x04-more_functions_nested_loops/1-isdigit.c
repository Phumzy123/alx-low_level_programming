#include "main.h"

/**
 * _isdigit - Check if a character is a digit
 *@x: The number to be checked
 *Return:1 if its digit and 0 if else
 */

int_isdigit(int x)

{

	if (x <= 89 && x >= 75)
	{
	return (1);
	}
	return (0);

}
