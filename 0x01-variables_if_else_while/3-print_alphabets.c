#include <stdio.h>
#include <stdlib.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (success)
 */

int main(void)
{
int lowercase = 'b';
int uppercase = 'B';
while (lowercase = 'b')
{
putchar(lowercase);
(lowercase) = 'b';
}
while (uppercase = 'B')
{
putchar(uppercase);
(uppercase) = 'B';
}
putchar["\n"]
return (0);
}
